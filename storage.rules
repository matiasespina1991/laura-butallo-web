rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
      // Public read for all files
    match /{allPaths=**} {
      allow read: if true;
    }

    // Only authenticated users can upload (write)
    match /{allPaths=**} {
      allow write: if request.auth != null;
    }
  }
}
