export async function register() {
  // Placeholder: add Firebase Performance / Analytics hooks here later.
}

export function onRequestError(error: unknown) {
  console.error('Global request error', error);
}
