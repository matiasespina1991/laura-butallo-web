# Laura Butallo · Web

This is the public-facing Next.js app. It used to live at the repo root and now sits under `apps/web`.

## Getting Started

```bash
npm install
npm run dev
```

## Scripts

- `npm run build`
- `npm run start`
- `npm run lint`
- `npm run format`

For Firebase Functions deploys, run the script from the repository root (`npm run deploy:functions`).
